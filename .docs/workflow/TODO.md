# TODO

- [ ] Continue Effect migration per `docs/specs/effect-migration.md`: Phase 3 (Effect handlers for `hello` / `surveys` routers, thin tRPC wrappers), Phase 4 (auth, settings, prompts), optional Phase 5 (replace tRPC HTTP with `@effect/platform`).
