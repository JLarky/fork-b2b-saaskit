# TODO

- [ ] create new branch/PR for migration to pnpm
- [ ] replace yarn with pnpm and update all mentions of yarn in docs
- [ ] revert back and compare performance before/after migration; maybe aad `npm install` and `bun install` to benchmark
