# TODO

- [x] create new branch/PR for migration to pnpm
- [x] replace yarn with pnpm and update all mentions of yarn in docs
- [ ] revert back and compare performance before/after migration; maybe add `npm install` and `bun install` to benchmark
