# TODO

- [ ] create new branch/PR for trpc -> orpc migration. The goal of orpc rewrite is to have openapi spec endpoing that could be used by other services. Make sure to use playwrite mcp to test as much of the app as possible end-to-end
