You might be running in one of the following environments:

- Local development on windows/linux/macos. In that case you need to ask user to install prerequisites and run the app.
- Virtual machine with sudo access. In that case you can install dependencies on your onw
- Cloud environment like Cursor or Codex. In that case you have access to `mise` command to pull dependencies that you need.
